# See https://www.dartlang.org/guides/libraries/private-files

# Files and directories created by pub
.dart_tool/
.packages
build/

# Directory created by dartdoc
# If you don't generate documentation locally you can remove this line.
doc/api/

# Avoid committing generated Javascript files:
*.dart.js
*.info.json      # Produced by the --dump-info flag.
*.js             # When generated by dart2js. Don't specify *.js if your
                 # project includes source files written in JavaScript.
*.js_
*.js.deps
*.js.map

# Sensitive / private documents (public repo)
# (career context now lives in docs/, which is gitignored below)
value_add.md
.DS_Store

# Generated resumes — kept local because they carry full contact PII
# (phone number, city). Rebuild from source with scripts/render-resumes.sh.
# See RESUME_PIPELINE.md for the architecture.
docs/
Resume.docx
Resume.zip

# Archived older resume versions — kept locally, out of the public repo
archive/

# Local build / preview artifacts
.playwright-mcp/
resume-*-preview*.png
.vscode/

# Raw camera originals — masters kept local; web-optimised WebP versions in
# portfolio/assets/images/photography/ are what get committed.
# Regenerate them with scripts/compress-photos.sh.
images_raw/

# Git worktrees
.worktrees/

cover_letters/*
