/// Base path for the Jaspr site.
///
/// Set via `Platform.environment['BASE_PATH']` in [main.server.dart].
/// Defaults to empty string (no prefix) for dev mode.
/// In production, set to `/jaspr` so links resolve correctly under the
/// Flutter portfolio site.
String basePath = '';
